package labs.lab5;

public interface Doable {
	
	/**
	 * 
	 * @param player	the player who is doing the doable action
	 * @return			a string message indicating what is being done and who is doing it
	 */
	String doIt(Player player);
}